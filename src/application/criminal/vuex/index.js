import actions from './actions';
import mutations from './mutations';

const state = {
    pagedTenants: { content: [] },
    criminal: {}
};

export default {
    state,
    actions,
    mutations
};
